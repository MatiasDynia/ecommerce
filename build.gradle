apply plugin: 'base'

group = 'com.md'
version = '1.0.0-SNAPSHOT'

subprojects {
	apply plugin: 'java'
	
	repositories {
		mavenCentral()
	}

	test {
		testLogging {
			events "passed", "skipped", "failed"
			exceptionFormat "full"
		}
	}
}

build.dependsOn ':ecommerce-commons:publish'